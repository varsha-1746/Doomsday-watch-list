# Character assets

Drop your own PNG or SVG files in this folder and map them in `src/data/characters.config.js`. The built-in silhouette layer is original line art and does not use official Marvel artwork.
